{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Debug com prompt-sync",
            "program": "${file}",
            "console": "integratedTerminal"
        }
    ]
}